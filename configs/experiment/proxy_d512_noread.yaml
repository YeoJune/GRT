# 두 승자 결합: d_model 512(천장↓) + 조건부읽기 제거(평지↓).
_base_: "proxy_baseline.yaml"

model:
  d_model: 512
  conditional_read: false

logging:
  save_dir: "checkpoints/proxy_d512_noread/"

wandb:
  run_name: "proxy-d512-noread"
  tags: ["improve", "proxy", "combo"]
  group: "improve"
