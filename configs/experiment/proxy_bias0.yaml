# 개선 후보 A: 쓰기 게이트 바이어스 -2.0 -> 0.0 (초반 레지스터 쓰기 억제 완화 → 평지 단축 가설).
_base_: "proxy_baseline.yaml"

model:
  register:
    write_gate_bias_init: 0.0

logging:
  save_dir: "checkpoints/proxy_bias0/"
  save_every_n_steps: 999999

wandb:
  run_name: "proxy-bias0"
  tags: ["improve", "proxy", "bias0"]
  group: "improve"
