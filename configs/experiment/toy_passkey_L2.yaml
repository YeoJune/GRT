# 사다리 L2: 어휘 1024. chance=ln(1024)=6.93, 목표<2.
_base_: "toy_passkey.yaml"

model:
  vocab_size: 1024

training:
  batch_size: 8
  warmup_steps: 200

rtla:
  enabled: false

wandb:
  run_name: "ladder-L2-vocab1024"
  tags: ["ladder", "L2"]
  group: "ladder"
