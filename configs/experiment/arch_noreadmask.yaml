# 아키텍처 개선 3: 조건부 읽기 마스크 제거 (ALU가 전체 S를 봄 — 읽기 게이트 병목 검증).
_base_: "proxy_baseline.yaml"

model:
  conditional_read: false

logging:
  save_dir: "checkpoints/arch_noreadmask/"

wandb:
  run_name: "arch-noreadmask"
  tags: ["improve", "proxy", "arch"]
  group: "improve"
