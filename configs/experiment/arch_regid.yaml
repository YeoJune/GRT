# 아키텍처 개선 1: 레지스터 슬롯별 identity 임베딩 (주소 지정 용이).
_base_: "proxy_baseline.yaml"

model:
  register_id: true

logging:
  save_dir: "checkpoints/arch_regid/"

wandb:
  run_name: "arch-regid"
  tags: ["improve", "proxy", "arch"]
  group: "improve"
